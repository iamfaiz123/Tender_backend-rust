-- Drop the user_roles table if it exists
DROP TABLE IF EXISTS user_roles;

-- Drop the users table if it exists
DROP TABLE IF EXISTS users;

-- Drop the roles enum type if it exists
DROP TYPE IF EXISTS roles;
